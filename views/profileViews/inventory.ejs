<nav>
  <a href="/profile">Back to the Prancing Pony</a>
  <a href="/characters">Your characters!</a>
  <a href="/characters/new">Create a new character!</a>
  <a href="/profile/shop">Visit Eginhard's Emporium</a>
  <a href="/profile/dealer">Talk to the Dealer</a>
  <a href="/profile/wealth">Head to Bodrig's Bank</a>
  <a href="/profile/explore">Explore Middle-earth!</a>
</nav>

<h1>TAKE STOCK OF YOUR INVENTORY</h1>

<% if (currentUserInv.length === 0) { %>
<p>Your stores are looking pretty meagre, my friend. Perhaps you wish to purchase some goods? Go and visit Eginhard's Emporium.</p>
<% } else { %>
<p>You have stores that even Smaug would envy, kind traveller. Here you can marvel at your good fortune. Or perhaps you would like to sell your goods? I hear there is a famous market at Bree...</p>
<%}%>

<div>
  <% if(strengthPotions.length !== 0) { %>
<p><%=strengthPotions[0].name%></p>
<p>You have <%=strengthPotions.length%> in your stores, worth a total of <%=strengthPotions[0].price * strengthPotions.length%> Castar</p>
<p><img src="<%= strengthPotions[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=strengthPotions[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(healthPotions.length !== 0) { %>
<p><%=healthPotions[0].name%></p>
<p>You have <%=healthPotions.length%> in your stores, worth a total of <%=healthPotions[0].price * healthPotions.length%> Castar</p>
<p><img src="<%= healthPotions[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=healthPotions[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(mithrils.length !== 0) { %>
<p><%=mithrils[0].name%></p>
<p>You have <%=mithrils.length%> in your stores, worth a total of <%=mithrils[0].price * mithrils.length%> Castar</p>
<p><img src="<%= mithrils[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=mithrils[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(breads.length !== 0) { %>
<p><%=breads[0].name%></p>
<p>You have <%=breads.length%> in your stores, worth a total of <%=breads[0].price * breads.length%> Castar</p>
<p><img src="<%= breads[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=breads[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(cloaks.length !== 0) { %>
  <p><%=cloaks[0].name%></p>
  <p><%=cloaks[0].price%> Castar</p>
  <p><img src="<%= cloaks[0].image %>" /></p>
  <p>You have <%=cloaks.length%> in your stores.</p>
  <% } %>
</div>
<div>
  <% if(canoes.length !== 0) { %>
<p><%=canoes[0].name%></p>
<p>You have <%=canoes.length%> in your stores, worth a total of <%=canoes[0].price * canoes.length%> Castar</p>
<p><img src="<%= canoes[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=canoes[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(bows.length !== 0) { %>
<p><%=bows[0].name%></p>
<p>You have <%=bows.length%> in your stores, worth a total of <%=bows[0].price * bows.length%> Castar</p>
<p><img src="<%= bows[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=bows[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(longSwords.length !== 0) { %>
<p><%=longSwords[0].name%></p>
<p>You have <%=longSwords.length%> in your stores, worth a total of <%=longSwords[0].price * longSwords.length%> Castar</p>
<p><img src="<%= longSwords[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=longSwords[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(shortSwords.length !== 0) { %>
<p><%=shortSwords[0].name%></p>
<p>You have <%=shortSwords.length%> in your stores, worth a total of <%=shortSwords[0].price * shortSwords.length%> Castar</p>
<p><img src="<%= shortSwords[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=shortSwords[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(staffs.length !== 0) { %>
<p><%=staffs[0].name%></p>
<p>You have <%=staffs.length%> in your stores, worth a total of <%=staffs[0].price * staffs.length%> Castar</p>
<p><img src="<%= staffs[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=staffs[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>
<div>
  <% if(axes.length !== 0) { %>
<p><%=axes[0].name%></p>
<p>You have <%=axes.length%> in your stores, worth a total of <%=axes[0].price * axes.length%> Castar</p>
<p><img src="<%= axes[0].image %>" /></p>
<form method="POST" action="/profile/inventory/<%=axes[0]._id%>">
  <button type="submit">Sell at the Market</button>
</form>
  <% } %>
</div>




 
